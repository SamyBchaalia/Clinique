class Patient {
    constructor(id, firstName, lastName,phoneNumber, birthday  ) {
            this.id = id;
            this.firstName = firstName;
            this.lastName = lastName;
            this.phoneNumber = phoneNumber;
            this.birthday=birthday;
    }
}

module.exports = Patient;